import React, { useState } from "react";
import styled from "styled-components";

const Div = styled.div`
  color: white;
  font-size: 2em;
`;

function Send(props) {
  return <Div>"https://melonkube.com"로 결과를 보냈습니다!</Div>;
}

export default Send;
