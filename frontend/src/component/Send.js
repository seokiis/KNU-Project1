import React, { useState } from "react";
import styled from "styled-components";

const Div = styled.div`
  color: white;
  font-size: 2em;
  text-align: center;
  font-size: 2em;
  font-weight: bold;
  line-height: 50px;
  font-family: "gmk";
`;
function Send(props) {
  return <Div>"https://sionPlot.com"로 결과를 보냈습니다!</Div>;
}

export default Send;
